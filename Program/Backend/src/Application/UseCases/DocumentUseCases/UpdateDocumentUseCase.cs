namespace Application.UseCases.DocumentUseCases;

public class UpdateDocumentUseCase
{
    // var document = await document.GetByIdAsync(Id) 
    //                 ?? throw new Exception($"Документа с таким Id:{Id} не существует.");
}